# Koha configuration file

# Koha Staff client port (default: 8080)
INTRAPORT=8080

# MariaDB root password (set your desired password here)
MYSQL_ROOT_PASSWORD=newpass

# Koha instance name (default: library)
KOHA_INSTANCE_NAME=library

# Apache listen ports (add 8080 for Koha staff client)
LISTEN_PORTS="8080"
